#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    /* --- Calculating circle area --- */

    // Vars to calculate
    /* float pi = 3.1416;
    float radio = 12;
    float resultAreaCircle;
    float resultVolumeSphere;

    // Applying the formula
    resultAreaCircle =  pi*(pow(radio,2));
    resultVolumeSphere =  (pi*4)*(pow(radio,2));

    // Printing the results
    cout << "Area circle = " << resultAreaCircle << endl;
    cout << "Volume sphere = " << resultVolumeSphere << endl;
    */

    /* --- cin --- */

    /* int age = 0;

   cout << "Enter your age" << endl;

   cin >> age;

   cout << "Your age: " << age << endl;
    */

    /* --- cin --- */

    

    return 0;
}